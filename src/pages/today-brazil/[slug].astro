---
import Layout from '../../layouts/Layout.astro';
import { supabase } from '../../lib/supabase';
import type { Article } from '../../lib/supabase';

export async function getStaticPaths() {
  const { data: articles } = await supabase
    .from('articles')
    .select('*')
    .order('published_date', { ascending: false });

  return (articles ?? []).map((article: Article) => ({
    params: { slug: article.slug },
    props: { article },
  }));
}

const { article } = Astro.props as { article: Article };

const publishedDate = new Date(article.published_date);
const formattedDateEn = publishedDate.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' });
const formattedDateZh = publishedDate.toLocaleDateString('zh-CN', { year: 'numeric', month: 'long', day: 'numeric' });
const formattedDatePt = publishedDate.toLocaleDateString('pt-BR', { day: 'numeric', month: 'long', year: 'numeric' });
---

<Layout
  title={article.title_en}
  description={article.excerpt_en}
  image={article.image_url || 'https://images.pexels.com/photos/3943716/pexels-photo-3943716.jpeg?auto=compress&cs=tinysrgb&w=1200&h=630&fit=crop'}
  type="article"
  publishedDate={article.published_date}
>
  <article class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <a href="/today-brazil" class="inline-flex items-center gap-2 text-brazil-green text-sm font-medium hover:underline mb-8">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
      <span data-lang-content="en">Back to Today's Brazil</span>
      <span data-lang-content="zh">返回今日巴西</span>
      <span data-lang-content="pt">Voltar ao Brasil Hoje</span>
    </a>

    <div class="flex items-center gap-3 mb-4">
      {article.tag && <span class="px-3 py-1 bg-green-100 text-brazil-green text-xs font-semibold rounded-full">{article.tag}</span>}
      <time class="text-sm text-gray-400">
        <span data-lang-content="en">{formattedDateEn}</span>
        <span data-lang-content="zh">{formattedDateZh}</span>
        <span data-lang-content="pt">{formattedDatePt}</span>
      </time>
    </div>

    <h1 class="font-display text-3xl sm:text-4xl font-bold text-brazil-blue leading-tight mb-6">
      <span data-lang-content="en">{article.title_en}</span>
      <span data-lang-content="zh">{article.title_zh}</span>
      <span data-lang-content="pt">{article.title_pt}</span>
    </h1>

    {article.image_url && (
      <img
        src={article.image_url}
        alt={article.title_en}
        class="w-full rounded-2xl mb-8 object-cover h-64"
      />
    )}

    <div class="prose prose-lg max-w-none text-gray-700 leading-relaxed space-y-5">
      <div data-lang-content="en" set:html={article.body_en} />
      <div data-lang-content="zh" set:html={article.body_zh} />
      <div data-lang-content="pt" set:html={article.body_pt} />
    </div>

    <div class="mt-12 pt-8 border-t border-gray-200 flex flex-col sm:flex-row justify-between gap-4">
      {article.prev_slug && (
        <a href={`/today-brazil/${article.prev_slug}`} class="group flex items-center gap-2 text-sm text-gray-500 hover:text-brazil-green">
          <svg class="w-4 h-4 group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
          <span data-lang-content="en">Previous Article</span>
          <span data-lang-content="zh">上一篇</span>
          <span data-lang-content="pt">Artigo Anterior</span>
        </a>
      )}
      {article.next_slug && (
        <a href={`/today-brazil/${article.next_slug}`} class="group flex items-center gap-2 text-sm text-gray-500 hover:text-brazil-green ml-auto">
          <span data-lang-content="en">Next Article</span>
          <span data-lang-content="zh">下一篇</span>
          <span data-lang-content="pt">Próximo Artigo</span>
          <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
        </a>
      )}
    </div>
  </article>
</Layout>
